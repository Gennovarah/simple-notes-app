services:
  backend:
    build: ./backend
    container_name: notes-api
    ports:
      - "5000:5000"
    restart: unless-stopped

  frontend:
    build: ./frontend
    container_name: notes-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
    restart: unless-stopped